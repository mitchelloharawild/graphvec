# Identify the children of a node in a graph

Identify the children of a node in a graph

## Usage

``` r
node_is_child(x, node, ...)
```

## Arguments

- x:

  A graph vector

- node:

  The root node from which children are identified
