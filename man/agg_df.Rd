% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agg_df.R
\name{agg_df}
\alias{agg_df}
\title{Create an aggregation table}
\usage{
agg_df(...)
}
\arguments{
\item{...}{Named \code{agg_vec} columns, all the same length.}
}
\value{
An \code{agg_df} object.
}
\description{
An \code{agg_df} is a table of \code{\link[=agg_vec]{agg_vec()}} columns, one row per level of a
(possibly crossed) aggregation structure -- e.g. \code{Purpose} and \code{State}
columns where some rows total one dimension, some the other, some both.
\code{\link[=nodes]{nodes()}}/\code{\link[=edges]{edges()}} reorient it into a graph: a row is a child of
another row whenever the parent aggregates exactly one more column and
matches on every other column's disaggregated value.
}
\examples{
agg_df(
  Purpose = agg_vec(c(NA, "Business", "Holiday"), c(TRUE, FALSE, FALSE)),
  State = agg_vec(c("NSW", NA, NA), c(FALSE, TRUE, TRUE))
)

}
